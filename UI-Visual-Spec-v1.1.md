UI 视觉规范 · 最终版（v1.2）
设计核心（不变）

神话 · 仪式 · 温暖 · 克制
有厚度，但不浮空
UI 是器物，不是特效

一、整体设计原则（最高约束，不可违反）

三条总原则：

不使用投影（Drop Shadow）

用“内在厚度”代替“外在阴影”

常态 UI 不发光，光只用于事件瞬间

以上三条为最高约束，
任何情况下不得为了“显眼 / 层级 / 炫”而破坏。

🆕 v1.2 新增｜适用前提声明

本规范基于中低复杂度、亮度受控的背景系统。
当背景过于复杂或对比过强时，应优化背景本身，
而不是通过给 UI 增加阴影、发光来补救。

二、圆角系统（冻结，不再讨论）

只允许以下 3 个数值：

按钮 / 小控件：16px

面板 / 卡片：24px

弹窗 / 主容器：32px

禁止出现其他圆角数值；
禁止“看着差不多就行”。

🆕 v1.2 新增｜执行规则

所有圆角元素 必须对齐整数像素

禁止非整数缩放（如 0.9x / 1.1x）

禁止因适配屏幕而临时修改圆角数值

三、厚度系统（统一方案）

统一厚度表达方式（全局唯一）：

Inner Stroke + Top Highlight

这是本项目唯一允许的立体表达方式。

1) 内描边（Inner Stroke）【必须】

用途
提供“边缘厚度”，解决贴片感，区分 UI 与背景。

规范参数

位置：inside

粗细：1px

颜色规则：比当前背景色深 6–8%，禁止纯黑

不透明度：100%

圆角：与元素本体一致

按钮 / 面板 / 弹窗：全部必须有内描边。

🆕 v1.2 新增｜移动端兜底规则

当 UI 元素与其背景亮度接近，
导致 1px 内描边在真机上不清晰时，
允许在不改变色相的前提下，将对比度提升至 8–10%。

⚠️ 此为兜底规则，不是常态设计标准。

2) 顶部高光（Top Highlight）【推荐】

用途
提供“受光感”，强调器物表面，而非发光。

规范参数

位置：元素顶部内侧

高度：2–3px

颜色：#FFFFFF

不透明度：5–8%

形式：实线，或 2px 内结束的轻渐变

规则

仅允许出现在顶部

禁止四边都有高光

🆕 v1.2 新增｜绘制约束

高光不可跨越圆角拐点

在圆角区域自然衰减收尾

禁止形成“白色描边感”

明确禁止（仍然有效）

Drop Shadow（任何形式）

外发光（常态）

底部暗边（当前版本禁用）

四、按钮系统（最终定案）
1) 按钮尺寸

高度：64px

内边距：左右 20–24px

图标：24px

适用于 1024×1920 竖屏移动端。

2) 主操作按钮

（如「开天地」）

背景：#FFF3DC

内描边：#E8D8B0

顶部高光：白色 6%

文案 / 图标：#6B5A2E

圆角：16px

3) 次级按钮（元素类）

背景：#FFF9EE

内描边：#EADFC5

顶部高光：白色 5%

文案 / 图标：#7A6A44

圆角：16px

4) 状态规范

Hover / Pressed

亮度 +5%

不位移

不缩放

不加阴影

🆕 v1.2 新增｜触屏反馈增强（推荐）

Pressed 状态下，允许同时：

顶部高光透明度 -2%

目的：
形成“受力 → 表面反射减弱”的物理反馈，
而非机械动画。

五、UI 面板 / 底部容器
1) 面板基础

背景：#FFF6E5

不透明度：95–100%

圆角：24px

2) 面板厚度

内描边：#E6D8B8

顶部高光：白色 5–6%

无阴影

3) 内边距

上下：24–28px

左右：24px

六、弹窗系统（最高层级）
1) 弹窗容器

背景：#FFFAF2

圆角：32px

内描边：#EADFC5

顶部高光：白色 6–8%

2) 遮罩层

颜色：#000000

不透明度：15–18%

无模糊

无渐变

🆕 v1.2 新增｜层级声明

弹窗 > 面板 > 常态 UI
层级差异只通过尺寸、圆角、亮度体现，
禁止通过阴影、发光制造层级。

七、光效与动效（严格限制）
允许

事件瞬间光效

持续：0.1–0.3s

位置：法阵中心 / 主按钮描边

用途：确认“发生了”

禁止

常态呼吸光

面板发光

列表光效

持续闪烁

八、文字与图标（补充）
文字颜色

主文案：#5B4A2F

次文案：#7A6A44

辅助：#A09372

禁用：#C8C1AD

图标规范

线性图标

不使用纯黑

推荐色：#6B5A2E

禁止图标自带阴影或发光